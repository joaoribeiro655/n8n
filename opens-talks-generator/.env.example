# Chave da Anthropic — usada SOMENTE no servidor (server/index.js).
# Nunca é exposta ao browser. Copie este arquivo para .env e preencha.
ANTHROPIC_API_KEY=sk-ant-...

# Porta do proxy Express (opcional, padrão 8787).
PORT=8787
